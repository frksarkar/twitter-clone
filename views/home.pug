extends main-page.pug

block append header
    link(rel="stylesheet", href="/style/home.css")
    
block mainBody
    +postCreate(true)
    .main-post-container
    +popupWindow()
    +deletePopupWindow()

    //- each val in postsData
    //- +submitPost(userData, val)
block script
    script(src="/js/home.js")


